---
// Henter parametrene fra Astro.props
const { variant = "primary", size = "small" } = Astro.props;

//Definer den styling (Tailwind classes, der gør sig gældende for alle knapper)
const buttonStyling = "border-1 px-6 py-3 font-heading uppercase";

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige varianter
const variantClasses = {
  primary: "border-black hover:bg-black hover:text-white" /* border-primary-500 */,
  secondary: "bg-black text-white hover:bg-white hover:text-black" /* border-secondary-500 */,
  third: "border-black bg-white text-black hover:bg-gray-200 hover:text-black" /* border-primary-500 */,
};

//Definer den styling (Tailwind classes), der gør sig gældende for de forskelligeog størrelser.
const sizeClasses = {
  large: "lg:text-computer-ctalarge text-mobile-ctalarge",
  small: "lg:text-computer-ctasmall text-mobile-ctasmall",
};
---

<!--Sæt classes for den pågældende knap sammen ved hjælp af Astros class:list -->
<button class:list={[buttonStyling, variantClasses[variant], sizeClasses[size]]}><slot /></button>
